import type { NextPage } from 'next';
import { Layout } from '../components';

const title: string = 'Ink JQ - Wordle';

const Wordle: NextPage = () => {
  return (
    <Layout _home={true} _pageTitle={title}>
      <></>
    </Layout>
  );
};

export default Wordle;
